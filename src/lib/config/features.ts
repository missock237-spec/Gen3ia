/** * Systeme de feature flags automatiques * Les fonctionnalites s'activent automatiquement * quand les variables d'env sont configurees */export type FeatureKey = | 'discord_bot' | 'slack_bot' | 'google_oauth' | 'github_oauth' | 'stripe_payments' | 'mobile_money' | 'saml_sso' | 'voice_ai' | 'documents';interface Feature { key: FeatureKey; name: string; description: string; envVars: string[]; active: boolean;}function checkEnv(vars: string[]): boolean { try { const g = globalThis as { process?: { env?: { [key: string]: string | undefined } } }; return vars.every(v => { const val = g?.process?.env?.[v]; return val !== undefined && val !== null && val !== ''; }); } catch { return false; }}const FEATURES: Feature[] = [ { key: 'discord_bot', name: 'Bot Discord', description: 'Integration du bot Discord pour les commandes slash', envVars: ['DISCORD_TOKEN', 'DISCORD_CLIENT_ID'], active: false, }, { key: 'slack_bot', name: 'Bot Slack', description: 'Integration du bot Slack pour les commandes et mentions', envVars: ['SLACK_SIGNING_SECRET', 'SLACK_BOT_TOKEN', 'SLACK_APP_TOKEN'], active: false, }, { key: 'google_oauth', name: 'Google OAuth', description: 'Connexion Gmail, Calendar et Drive via OAuth', envVars: ['GOOGLE_CLIENT_ID', 'GOOGLE_CLIENT_SECRET'], active: false, }, { key: 'github_oauth', name: 'GitHub OAuth', description: 'Authentification GitHub et actions sur les repos', envVars: ['GITHUB_CLIENT_ID', 'GITHUB_CLIENT_SECRET'], active: false, }, { key: 'stripe_payments', name: 'Paiements Stripe', description: 'Traitement des paiements par carte', envVars: ['STRIPE_CLIENT_ID', 'STRIPE_SECRET_KEY'], active: false, }, { key: 'mobile_money', name: 'Mobile Money', description: 'Paiements Orange Money et MTN Mobile Money', envVars: ['MOBILE_MONEY_API_KEY'], active: false, }, { key: 'saml_sso', name: 'SSO Enterprise', description: 'Authentification SSO SAML pour les entreprises', envVars: ['SAML_ISSUER', 'SAML_CERT'], active: false, }, { key: 'voice_ai', name: 'Voice AI', description: 'Reconnaissance vocale et synthese', envVars: ['VOICE_API_KEY'], active: false, }, { key: 'documents', name: 'Generation de documents', description: 'Generation de rapports, CV et documents', envVars: ['DOCUMENT_API_KEY'], active: false, },];function evaluateFeatures(): Feature[] { return FEATURES.map(f => ({ ...f, active: checkEnv(f.envVars), }));}export function isFeatureActive(key: FeatureKey): boolean { const feature = FEATURES.find(f => f.key === key); if (!feature) return false; return checkEnv(feature.envVars);}export function getFeatures(): Feature[] { return evaluateFeatures();}export function getActiveFeatures(): Feature[] { return evaluateFeatures().filter(f => f.active);}export function getInactiveFeatures(): Feature[] { return evaluateFeatures().filter(f => !f.active);}export function requireFeature(key: FeatureKey): { ok: boolean; message: string } { const feature = FEATURES.find(f => f.key === key); if (!feature) return { ok: false, message: 'Fonctionnalite inconnue' }; if (!isFeatureActive(key)) { return { ok: false, message: `🔒 ${feature.name} inactif. Configurez: ${feature.envVars.join(', ')}` }; } return { ok: true, message: '' };}export type { Feature };