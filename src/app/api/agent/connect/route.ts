import { NextRequest, NextResponse } from 'next/server';import { agentOrchestrator } from '@/lib/agent/agent-orchestrator';import { isFeatureActive } from '@/lib/config/features';export async function POST(request: NextRequest) {  try {    const { userId, platform, authCode } = await request.json();    if (!userId || !platform || !authCode) {      return NextResponse.json({ error: 'userId, platform et authCode requis' }, { status: 400 });    }    const platformFeatures: Record<string, string> = {      gmail: 'google_oauth', google_calendar: 'google_oauth', google_drive: 'google_oauth',      slack: 'slack_bot', discord: 'discord_bot', github: 'github_oauth', stripe: 'stripe_payments',    };    const featureKey = platformFeatures[platform];    if (featureKey && !isFeatureActive(featureKey as 'google_oauth' | 'slack_bot' | 'discord_bot' | 'github_oauth' | 'stripe_payments')) {      return NextResponse.json({ error: 'Plateforme non configuree', message: 'Cette plateforme nest pas active', platform }, { status: 503 });    }    const success = await agentOrchestrator.connect(userId, platform);    return NextResponse.json({ success, message: success ? 'Plateforme connectee' : 'Echec de connexion' });  } catch (error: unknown) {    return NextResponse.json({ error: error instanceof Error ? error.message : 'Erreur inconnue' }, { status: 500 });  }}export async function GET(request: NextRequest) {  const { searchParams } = new URL(request.url);  const userId = searchParams.get('userId');  const platform = searchParams.get('platform') as string | null;  if (!userId || !platform) return NextResponse.json({ error: 'userId et platform requis' }, { status: 400 });  const appUrl = process.env.NEXT_PUBLIC_APP_URL || 'http://localhost:3000';  const url = agentOrchestrator.getOAuthURL(platform as any, appUrl + '/api/agent/oauth/callback');  return NextResponse.json({ url, requiresConfig: !isFeatureActive('google_oauth') });}