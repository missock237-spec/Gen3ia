export function validateString(val: unknown, maxLen = 500) { if (typeof val !== 'string') return null; const t = val.trim(); if (t.length === 0 || t.length > maxLen) return null; return t; }export function sanitizeHtml(input: string) { return input.replace(/</g, '&lt;').replace(/>/g, '&gt;'); }export function validateEmail(email: string) { return /^[^s@]+@[^s@]+\.[^s@]+$/.test(email); }