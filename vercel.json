{
  "name": "gen3ia",
  "version": 2,
  "framework": "nextjs",
  "rootDirectory": "apps/web",
  "buildCommand": "cd ../.. && npm install -g pnpm && pnpm install --frozen-lockfile && cd apps/web && pnpm build",
  "installCommand": "cd ../.. && npm install -g pnpm && pnpm install --frozen-lockfile",
  "outputDirectory": ".next",
  "regions": ["fra1"],
  "cleanUrls": true,
  "trailingSlash": false,
  "github": {
    "silent": true,
    "autoJobCancelation": true
  }
}
