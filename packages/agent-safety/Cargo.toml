[package]
name = "agent-safety"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", features = ["napi4"] }
napi-derive = "2"
regex = "1"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
