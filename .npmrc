# Gen3ia npm config
# Empêcher les shasums pour accélérer l'installation
package-lock=false

# Mode strict pour éviter les problèmes de résolution
engine-strict=true

# Legacy peer deps pour compatibilité Next.js
legacy-peer-deps=true
