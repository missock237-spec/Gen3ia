// OBSOLÈTE — La source de vérité est `next.config.js`.
// Ce fichier a été retiré pour éviter tout conflit (Next.js préfère .ts sur .js).
// Ne pas utiliser. Suppression recommandée : `git rm next.config.ts`

export const config = null;
